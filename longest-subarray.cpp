class Solution{
public:
    int longestSubarray(vector<int> &nums, int k){
        int maxi=0;

        for (int i=0;i<nums.size();i++)
        {
            
            long long sum=0;
            sum += nums[i];

            if(sum==k);
        }
    }
};
